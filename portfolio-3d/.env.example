# Copy to .env.local and fill in values. Never commit .env.local.

# Absolute base URL used for Open Graph canonical image URLs.
# Example: https://yourname.dev
NEXT_PUBLIC_SITE_URL=

# Optional: email endpoint for the contact form (if using a serverless route)
# CONTACT_API_KEY=
